export interface counterType {
  counter: {
    value: number;
  };
}

export interface loginType {
  login: {
    isLogin: boolean;
  };
}
export interface themeType {
  theme: {
    isDark: boolean;
  };
}
